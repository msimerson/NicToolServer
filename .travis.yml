language: perl
perl:
# - "5.22"
  - "5.18"
# - "5.10"

services:
    - mysql

addons:
  apt:
    packages:
    - expat
    - gettext
    - apache2
    - apache2.2-common
    - apache2-mpm-worker
    - libapache2-mod-perl2
    - libapache2-mod-perl2-dev
    - libdigest-hmac-perl
    - libjson-perl
    - librpc-xml-perl
    - libsoap-lite-perl
    - libmodule-build-perl
    - libmime-base32-perl
    - libmime-base64-perl
    - libbind-confparser-perl
    - libssl-dev
    - libcrypt-openssl-rsa-perl
    - libcrypt-openssl-dsa-perl
    - libyaml-perl
    - bind9utils
    - libwww-perl
    - liburi-perl
    - libmime-tools-perl
    - libmailtools-perl
    - libdist-zilla-perl
    - libfile-sharedir-perl
    - libperl-prereqscanner-perl
    - libxml-libxml-perl
    - libapache-dbi-perl
    - libdbix-simple-perl
    - libxml-parser-perl
    - libdbi-perl
    - libnet-ip-perl
    - libnet-dns-perl

before_install:
#   - sudo .test/tls-setup.sh
#   - sudo apt-get install -y libextutils-makemaker-perl || echo "nope"
    - curl -L https://cpanmin.us | sudo perl - --sudo App::cpanminus
      # global/system installed perl modules
#   - sudo cpanm --notest CGI RPC::XML Time::TAI64 CryptX Crypt::Mac::HMAC Crypt::KeyDerivation DBD::mysql Test::HTML::Lint Apache::DBI Shell Net::DNS::ZoneFile LWP::UserAgent
#   - sudo cpanm --notest JSON SOAP::Lite XML::Parser Net::DNS MIME::Base32 Net::IP BIND::Conf_Parser DBIx::Simple DBI

install:
    - sudo cp .test/apache-nictool.conf /etc/apache2/sites-enabled/
    - sudo service apache2 restart || sudo cat /var/log/apache2/error.log
    - sudo perl .test/create_tables.pl

before_script:

script:

after_success:
after_failure:
    - pwd
    - cat /var/log/apache2/error.log
    - cat /etc/apache2/apache2.conf
    - ls /usr/lib/apache2/modules/
after_script:

#sudo: false
